import { AuthContext } from "./context/auth-context";
import { fetch_url_get } from "./function/common-function/fetch";

export const WishlistCategoriesList = (color, title, navigation, route) => {

    
}
